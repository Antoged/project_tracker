PORT=4000
# Замените на вашу реальную строку подключения к PostgreSQL
# Формат: postgres://пользователь:пароль@хост:порт/база_данных
DATABASE_URL=postgres://postgres:ВАШ_ПАРОЛЬ@localhost:5432/project_tracker
# Замените на случайную строку для продакшена (минимум 32 символа)
JWT_SECRET=supersecret
# Для разработки можно оставить true, для продакшена обязательно false
DEV_BYPASS_AUTH=true

