# Как сделать сайт доступным для любого человека в мире 🌍

Чтобы ваш сайт был доступен из интернета для всех, нужно задеплоить его на хостинг.

## Что нужно сделать:

1. **Frontend** → задеплоить на Vercel (бесплатно)
2. **Backend** → задеплоить на Render (бесплатно)
3. **База данных** → создать PostgreSQL на Render (бесплатно)

---

## Шаг 1: Подготовка проекта

### 1.1. Создайте GitHub репозиторий (если еще нет)

```powershell
# В корне проекта
git init
git add .
git commit -m "Initial commit"
```

Затем создайте репозиторий на GitHub и загрузите код:
```powershell
git remote add origin https://github.com/ВАШ_USERNAME/ВАШ_РЕПОЗИТОРИЙ.git
git branch -M main
git push -u origin main
```

### 1.2. Подготовьте backend для продакшена

Backend уже готов! Скрипт `start` есть в `package.json`.

---

## Шаг 2: Деплой Backend на Render

### 2.1. Создайте аккаунт на Render

1. Перейдите на [render.com](https://render.com)
2. Зарегистрируйтесь через GitHub (проще всего)

### 2.2. Создайте PostgreSQL базу данных

1. В Dashboard нажмите **"New +"** → **"PostgreSQL"**
2. Название: `project-tracker-db`
3. План: **Free** (бесплатный)
4. Регион: выберите ближайший
5. Нажмите **"Create Database"**
6. **ВАЖНО:** Скопируйте **"Internal Database URL"** (он понадобится позже)

### 2.3. Создайте Web Service для Backend

1. В Dashboard нажмите **"New +"** → **"Web Service"**
2. Подключите ваш GitHub репозиторий
3. Настройки:
   - **Name:** `project-tracker-backend`
   - **Region:** выберите тот же регион, что и база данных
   - **Branch:** `main`
   - **Root Directory:** `backend`
   - **Runtime:** `Node`
   - **Build Command:** `npm install && npm run build`
   - **Start Command:** `npm start`
   - **Plan:** **Free**

4. **Environment Variables** (переменные окружения):
   - `PORT` = `4000`
   - `DATABASE_URL` = вставьте **Internal Database URL** из базы данных
   - `JWT_SECRET` = любая случайная строка (например, `my-super-secret-key-12345`)
   - `DEV_BYPASS_AUTH` = `false` (важно для продакшена!)

5. Нажмите **"Create Web Service"**

6. **Подождите 2-3 минуты** пока соберется и запустится сервис

7. После запуска Render даст вам URL, например: `https://project-tracker-backend.onrender.com`

8. Проверьте работу: откройте `https://ВАШ-URL.onrender.com/health` - должно вернуться `{"ok":true,"time":"..."}`

---

## Шаг 3: Деплой Frontend на Vercel

### 3.1. Создайте аккаунт на Vercel

1. Перейдите на [vercel.com](https://vercel.com)
2. Зарегистрируйтесь через GitHub

### 3.2. Подготовьте frontend

Создайте файл `frontend/vercel.json`:

```json
{
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
```

Это нужно для правильной работы React Router.

### 3.3. Задеплойте проект

1. В Vercel Dashboard нажмите **"Add New..."** → **"Project"**
2. Импортируйте ваш GitHub репозиторий
3. Настройки:
   - **Framework Preset:** Vite
   - **Root Directory:** `frontend`
   - **Build Command:** `npm run build`
   - **Output Directory:** `dist`

4. **Environment Variables** (переменные окружения):
   - `VITE_API_URL` = `https://ВАШ-BACKEND-URL.onrender.com/api`
     (замените на URL вашего backend с Render)

5. Нажмите **"Deploy"**

6. **Подождите 1-2 минуты** пока соберется проект

7. Vercel даст вам URL, например: `https://project-tracker.vercel.app`

---

## Шаг 4: Проверка работы

1. Откройте URL от Vercel в браузере
2. Сайт должен открыться и работать!
3. Создайте тестовый проект - должно работать

---

## Шаг 5: Настройка CORS (если нужно)

Если возникают ошибки CORS, обновите `backend/src/server.ts`:

```typescript
app.use(cors({
  origin: ['https://ВАШ-FRONTEND-URL.vercel.app', 'http://localhost:5173'],
  credentials: true
}));
```

---

## Важные моменты:

### Бесплатные планы имеют ограничения:

**Render (Backend):**
- Сервис "засыпает" после 15 минут бездействия
- Первый запрос после "сна" может занять 30-50 секунд
- Это нормально для бесплатного плана

**Vercel (Frontend):**
- Ограничений практически нет
- Работает быстро и стабильно

**Render PostgreSQL:**
- 90 дней бесплатно, потом нужно платить или мигрировать
- Для постоянного использования рассмотрите [Supabase](https://supabase.com) (бесплатно навсегда)

---

## Альтернативные варианты:

### Railway (вместо Render)
- [railway.app](https://railway.app) - похож на Render, тоже бесплатный план
- Может быть быстрее

### Netlify (вместо Vercel)
- [netlify.com](https://netlify.com) - альтернатива Vercel
- Тоже бесплатно

### Supabase (вместо Render PostgreSQL)
- [supabase.com](https://supabase.com) - бесплатный PostgreSQL навсегда
- Больше места и функций

---

## Если что-то не работает:

1. **Проверьте логи:**
   - Render: в Dashboard → ваш сервис → Logs
   - Vercel: в Dashboard → ваш проект → Deployments → View Function Logs

2. **Проверьте переменные окружения:**
   - Убедитесь, что все переменные заданы правильно
   - Особенно `DATABASE_URL` и `VITE_API_URL`

3. **Проверьте URL:**
   - Backend URL должен быть доступен (откройте `/health`)
   - Frontend должен использовать правильный backend URL

4. **Подождите:**
   - Render может занять несколько минут для первого запуска
   - После "сна" первый запрос может быть медленным

---

## Готово! 🎉

Теперь ваш сайт доступен по адресу от Vercel для любого человека в мире!
